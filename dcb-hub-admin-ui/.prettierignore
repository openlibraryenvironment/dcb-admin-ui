# Ignore artifacts:
build
coverage

# Generated by the TanStack router plugin on every build, in its own style (single
# quotes, no semicolons). Prettier reformatting it means the next build reverts all
# 1811 lines - the file's own header asks to be excluded from formatters. ESLint
# already ignores it; see eslint.config.mjs.
src/routeTree.gen.ts
